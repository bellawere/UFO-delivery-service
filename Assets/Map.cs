using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Map : MonoBehaviour
{
    //Use this script to control variant regions for storm, rain, heatwave, hurricane
    //Use it also for fixed regions of desert, volcan, and snow
    //This script should only return values for regions and the minimap itself

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
